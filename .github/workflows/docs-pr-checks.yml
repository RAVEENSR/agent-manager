name: Documentation Site PR Checks

on:
  pull_request:
    branches:
      - main
    paths:
      - 'documentation/**'
      - '.github/workflows/docs-pr-checks.yml'

# Both jobs only check out the repository and run npm; nothing needs write access.
permissions:
  contents: read

jobs:
  audit:
    name: Security Audit
    runs-on:
      - codebuild-wso2_agent-manager-${{ github.run_id }}-${{ github.run_attempt }}

    steps:
      - uses: actions/checkout@v6
      - uses: actions/setup-node@v6
        with:
          node-version: 20
          cache: npm
          cache-dependency-path: documentation/package-lock.json

      - name: Install dependencies
        working-directory: ./documentation
        run: npm ci

      - name: Security Audit
        working-directory: ./documentation
        run: npm run audit

  build:
    name: Build Documentation Site
    runs-on:
      - codebuild-wso2_agent-manager-${{ github.run_id }}-${{ github.run_attempt }}

    steps:
      - uses: actions/checkout@v6
        with:
          fetch-depth: 0
      - uses: actions/setup-node@v6
        with:
          node-version: 20
          cache: npm
          cache-dependency-path: documentation/package-lock.json

      - name: Install dependencies
        working-directory: ./documentation
        run: npm ci

      - name: Build documentation
        working-directory: ./documentation
        run: npm run build
