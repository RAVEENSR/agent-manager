{{/*
Runs the production secret guard. Emits no manifest; only fails the render
when agentManagerService.requireExternalSecrets is set and a credential is
still on a committed literal default. See _validate.tpl.
*/}}
{{- include "agent-management-platform.validateProdSecrets" . -}}
